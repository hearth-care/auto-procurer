# [Plan] Doc truth: retire the stub-worker claims

- **Date:** 2026-07-02 · **Branch:** `claude/plan-doc-truth` · **Status:** plan — implementation lands on this same branch per the fleet dispatch protocol
- **Type:** docs-only planning artifact (this file is the binding spec + implementation plan)
- **Related:** `docs/plans/2026-06-fleet-audit-readme-runtime-refresh.md` (PR #16 — refreshed the README when four builders existed; three more shipped since)

> **For agentic workers:** REQUIRED SUB-SKILL: implement this plan task-by-task (Claude: `superpowers:subagent-driven-development` or `superpowers:executing-plans`; Codex: same phase/TDD/verification discipline). Steps use checkbox (`- [ ]`) syntax for tracking. Tick checkboxes as work lands and commit this plan with the code. Keep `## HANDOFF NOTES` current.

## Context (current-state evidence, verified against `origin/main` @ `db5fbc8`)

Three repo docs still describe a scaffold that no longer exists. Each stale claim below was
re-verified against code this week; the code citations are the truth source.

Baseline gates on `origin/main`: `uv run pytest -q` → **286 passed**.

## Goal

Every claim in `CLAUDE.md`, `README.md`, and `pyproject.toml` matches the shipped code, so an
agent or operator reading only the docs gets the real worker: P1–P4 procurement surfaces,
seven horizon Signal builders, an invoice CLI, Cloud Run runtime, and CI-gated (not
pre-commit-gated) checks.

## Non-goals

- No behaviour changes; no new commands, walks, tests of new features, or CI changes.
- Not re-documenting the cockpit journeys (owned by `docs/cockpit-journeys.md`).
- Not documenting the `request list` / `book …` commands — those ship with the separate
  operator-surfaces plan PR (`claude/plan-operator-surfaces`), which owns its own README lines.
  If that PR lands first, leave its lines untouched; the two PRs edit different claims and a
  rebase is textual only.
- Not scrubbing historical test fixtures — this PR touches docs + the one dead pyproject marker.

## Stale claims — the full set (HR5; each is a checkbox in the Tasks)

| # | file:line (@ `db5fbc8`) | stale claim | truth (evidence) |
|---|---|---|---|
| 1 | `CLAUDE.md:5-6` | "xsource is a stub worker: it has no domain rules yet. Add them … as the worker grows." | P1–P4 shipped: research→shortlist walks, draft-only outreach/follow-up, reply watcher, sheet sync, invoice capture/handoff (`src/xsource/cli/cockpit.py::register_all`, `src/xsource/invoices/`, `src/xsource/p4/`); Cloud Run runtime (`.github/workflows/deploy-cloud-run.yml`, `docs/runbooks/cloud-run-cutover.md`); 286-test suite. Domain rules exist and are enforced by tests (`tests/test_no_send_endpoints.py`, `tests/test_cockpit_placeholders.py`). |
| 2 | `README.md:62-75` | "`xsource signals scan` calls `scan_xsource_horizon`, composed from **four** horizon builders" + a 4-bullet list | **Seven** builders composed at `src/xsource/signals/build.py:369-414`: the four listed **plus** `build_payment_required_signals` (:260), `build_invoice_variance_signals` (:292), `build_rejected_invoice_signals` (:334). `grep -c "^def build_" src/xsource/signals/build.py` → `7`. |
| 3 | `README.md:25-37` ("CLI surface" fence) | Lists signals/watcher/request commands only — the `xsource invoice` group is absent | `src/xsource/cli/invoice.py` registers `add`, `import`, `list`, `reemit`, `write-off`, `sync-acks`; mounted at `src/xsource/cli/__init__.py:30`. |
| 4 | `README.md:135-137` | "Pre-commit runs ruff, ruff-format, and mypy on every commit." | No `.pre-commit-config.yaml` exists anywhere in the repo (`ls -a` at root; `git ls-files '*pre-commit*'` → empty). The actual gates are CI's (`.github/workflows/ci.yml`): `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src`, `uv run pytest -q`. The Development block also omits the mypy/format-check commands CI runs. |
| 5 | `pyproject.toml:44-46` | `markers = ["horizon_stub: marks the xfail test guarding the empty @scan_horizon stub"]` | No test uses the marker: `grep -rn horizon_stub tests/ src/` → zero hits (only pyproject itself). `scan_xsource_horizon` is real and composed (`tests/test_signals_build.py:3-5` says so explicitly). Dead scaffold metadata. |
| 6 | `README.md:101-125` (config table) | Table presents itself as the knob list but omits eight `XSOURCE_*` knobs plus `COMPANIES_HOUSE_API_KEY` | `src/xsource/config.py:46-66` also reads `XSOURCE_STATE_DIR`, `XSOURCE_SHORTLIST_N`, `XSOURCE_MAX_PLACES_CALLS`, `XSOURCE_MAX_WEB_SEARCHES`, `XSOURCE_POLL_SECONDS`, `XSOURCE_MAX_BACKOFF_SECONDS`, `XSOURCE_BREAKER_THRESHOLD`, `XSOURCE_RESEARCH_MODEL`, `XSOURCE_OPERATOR_DISPLAY_NAME`; `src/xsource/wiring.py:59` reads `COMPANIES_HOUSE_API_KEY`. |
| 7 | `src/xsource/cli/__init__.py:4-8` (module docstring) | "Sub-apps: * `xsource signals scan` …" — names only the signals sub-app | Four sub-apps are mounted (`signals`, `watcher`, `request`, `invoice`) at `src/xsource/cli/__init__.py:27-30`. |

**Claims audited and verified still true (no change needed — listed so QA knows the audit
covered them):** quick-start outputs incl. the literal
`signals: disabled (set XSOURCE_EMIT_SIGNALS=1 to enable)` (`src/xsource/cli/signals.py:39`);
`watcher run --once/--cycles/--interval` flags (`src/xsource/cli/watcher.py:111-115`); the
Runtime/Cloud Run section (workflow + `deploy/xsource-cloud-run.env.example` +
`scripts/cutover_launchd.py` all exist); the shelf table A–E,G (matches `_SHELVES`,
`src/xsource/cli/cockpit.py:40-47`); the write-gate/draft-never-send paragraph; the existing
config-table defaults (radius 15, budget 10, chase 3, chain `claude-sonnet-4-6`, prefix
`state/xsource` — `src/xsource/config.py`); the `pyproject.toml` description's "scaffolded
from clonway-cockpit's worker template" (historical provenance, not a state claim).

## Binding replacement text

**CLAUDE.md** — replace exactly the two stale sentences ("xsource is a stub worker … as the
worker grows.") with (keep the surrounding framework-rules sentence and the whole "Bumping the
framework pin" section untouched):

```markdown
xsource is a live worker: procurement research → shortlist Sheet → draft-only
outreach and follow-ups → reply watcher → sheet sync → invoice capture/handoff,
plus horizon Signal builders, running as Cloud Run jobs (see README "Runtime").

## Domain rules

- **Draft-never-send:** outreach surfaces create Gmail drafts and record ids;
  nothing in this repo sends email (`tests/test_no_send_endpoints.py` is the gate).
- **Single write gate:** every mutating cockpit walk routes through the framework
  `confirm_apply`; agent mode (`--agent-stdio`) is dry-run without `--allow-apply`.
- **Public repo:** no real supplier names/ids, personal emails, internal hostnames,
  or machine-local paths in docs or examples.
- **Cockpit tests drive frames, not text:** assert structured `ScreenModel` frames
  (`CockpitDriver`) or registry data — never `export_text()` scraping
  (`tests/test_cockpit_placeholders.py` shows the pattern).
```

**README signals section** — the sentence becomes "…composed from seven horizon builders:";
keep the four existing bullets verbatim and append (wording pinned to the builders' actual
behaviour at the cited lines):

```markdown
- `build_payment_required_signals` — emits a payment-required signal for every
  invoice still in `captured`/`emitted`/`re-emitted`, escalating to error when
  the due date has passed.
- `build_invoice_variance_signals` — emits a review signal when an invoice
  carries an unresolved quoted-vs-invoiced variance.
- `build_rejected_invoice_signals` — emits a fix-it signal for each rejected
  invoice, pointing at `xsource invoice reemit`/`write-off`.
```

**README CLI surface fence** — append after the existing `request …` lines:

```
xsource invoice add           # record a supplier invoice (never pays money)
xsource invoice import        # bulk-import invoice rows from a CSV
xsource invoice list          # list captured invoices
xsource invoice reemit        # correct a rejected invoice and re-emit it
xsource invoice write-off     # mark a rejected invoice written off
xsource invoice sync-acks     # ingest payment-required acknowledgements
```

**README Development section** — delete the pre-commit sentence; the block becomes:

````markdown
```bash
uv sync
uv run pytest -q                # full test suite
uv run ruff check .             # linting
uv run ruff format --check .    # formatting (CI mode; drop --check to write)
uv run mypy src                 # types
```

These four are exactly what CI runs on every push to `main` and in the merge
queue (`.github/workflows/ci.yml`). There are no pre-commit hooks in this repo —
run the gates yourself before pushing.
````

**README config table** — append these rows (defaults from `src/xsource/config.py:46-66`,
`src/xsource/wiring.py:59`; all optional):

```markdown
| `XSOURCE_STATE_DIR` | no | `~/.claude-inbox/xsource/state` | Local state directory (hydrated from GCS in Cloud Run) |
| `XSOURCE_SHORTLIST_N` | no | `5` | Shortlist size per request |
| `XSOURCE_MAX_PLACES_CALLS` | no | `10` | Places-search call cap per research run |
| `XSOURCE_MAX_WEB_SEARCHES` | no | `8` | Web-search call cap per research run |
| `XSOURCE_POLL_SECONDS` | no | `60` | Watcher poll interval |
| `XSOURCE_MAX_BACKOFF_SECONDS` | no | `300` | Watcher error-backoff ceiling |
| `XSOURCE_BREAKER_THRESHOLD` | no | `10` | Watcher circuit-breaker threshold |
| `XSOURCE_RESEARCH_MODEL` | no | — | Single-model fallback when `XSOURCE_MODEL_CHAIN` is unset |
| `XSOURCE_OPERATOR_DISPLAY_NAME` | no | `Milo` | Name signed into follow-up drafts |
| `COMPANIES_HOUSE_API_KEY` | no | — | Enables the Companies House sanity check during research |
```

**pyproject.toml** — delete the whole `markers = [...]` block (lines 44-46); keep
`addopts = "-ra --strict-markers"` (with `--strict-markers`, the green suite proves no test
still declares `horizon_stub`).

**`src/xsource/cli/__init__.py` docstring** — list the sub-apps actually mounted at
implementation time (today: `signals`, `watcher`, `request`, `invoice`; if the
operator-surfaces PR has merged first, `book` too — mirror `app.add_typer` calls, don't
pre-announce).

## Safety invariants / figures

- No behaviour changes: the only non-doc edits are the pyproject marker block and one module
  docstring. Cell-table/HR3 and rendered-behaviour/HR8 tests are **n/a — no new behaviour**;
  the regression net is the full suite under `--strict-markers` (it fails collection if any
  test still used the removed marker) plus the greps below, which QA re-runs verbatim.
- Figures pinned (HR7): builder count **7** = `grep -c "^def build_" src/xsource/signals/build.py`;
  invoice sub-commands **6** = the `@invoice_app.command` decorators in
  `src/xsource/cli/invoice.py`; baseline **286 passed**.
- One source of truth (HR6): every number/name added to the docs carries its code citation in
  the table above; where a value would otherwise be stated twice in docs (builder list in
  README vs CLAUDE.md), CLAUDE.md deliberately avoids the count and points at README.

## Global constraints

- Public repo: no internal hostnames, personal emails, or machine-local paths in any edited doc.
- **Not operator-facing (HR1): this PR changes no command, key, flag, provisioning step, or
  operating rhythm — documentation and dead-metadata removal only. No RUNBOOK DELTA.**
- **Depends on:** nothing unmerged — no wave tag (HR11). Textual-only adjacency with
  `claude/plan-operator-surfaces` (see Non-goals).
- **Gates (HR2 — exact commands from `.github/workflows/ci.yml`, run from repo root, paste
  tails in DONE):** `uv run pytest -q` · `uv run ruff check .` · `uv run ruff format --check .`
  · `uv run mypy src`

---

## Tasks

### Task 1: CLAUDE.md — retire the stub-worker claim

**Files:** modify `CLAUDE.md`.

- [x] Replace the two stale sentences with the binding CLAUDE.md text above (claim #1). Keep
  the first framework-rules sentence and the "Bumping the framework pin" section byte-identical.
- [x] Verify: `grep -n "stub worker" CLAUDE.md` → no output; `grep -n "Domain rules" CLAUDE.md`
  → one hit. Paste both.
- [x] **Commit:** `docs: CLAUDE.md describes the live worker + its domain rules`

### Task 2: README — seven builders, invoice CLI, real gates, full config table

**Files:** modify `README.md`.

- [x] Claim #2: "four" → "seven"; append the three pinned builder bullets.
- [x] Claim #3: append the six pinned `xsource invoice …` lines to the CLI-surface fence.
- [x] Claim #4: replace the Development block with the binding text (pre-commit sentence gone;
  four CI-exact commands present).
- [x] Claim #6: append the ten pinned config rows.
- [x] Verify (paste output):
  `grep -n "seven horizon builders" README.md` → 1 hit;
  `grep -c "xsource invoice" README.md` → ≥ 6;
  `grep -n "re-commit" README.md` → no output;
  `grep -n "XSOURCE_OPERATOR_DISPLAY_NAME\|COMPANIES_HOUSE_API_KEY" README.md` → 2 hits.
- [x] **Commit:** `docs: README matches shipped signals/CLI/gates/config`

### Task 3: pyproject marker + CLI docstring

**Files:** modify `pyproject.toml`, `src/xsource/cli/__init__.py`.

- [ ] Claim #5: delete the `markers` block from `[tool.pytest.ini_options]`; leave `addopts`
  untouched.
- [ ] Claim #7: update the module docstring's sub-app list to mirror the `app.add_typer` calls
  present on the branch after rebase.
- [ ] Verify: `grep -rn "horizon_stub" .` (repo root, excluding `.git`) → no output; then run
  `uv run pytest -q` — green under `--strict-markers` proves the marker was truly unused.
- [ ] **Commit:** `chore: drop dead horizon_stub marker; cli docstring lists real sub-apps`

### Task 4: full gates + DONE

- [ ] Run all four gates verbatim from the repo root; paste output tails in the DONE comment:
  `uv run pytest -q` (expect 286 passed — no test changes) · `uv run ruff check .` ·
  `uv run ruff format --check .` · `uv run mypy src`.
- [ ] Re-run every per-task verification grep in one block and paste it.
- [ ] `gh pr ready` + flip label to `agent:needs-qa` + one DONE comment (shipped work,
  deviations, gate evidence). State explicitly: "Not operator-facing — no RUNBOOK DELTA."

---

## Self-Review

- **Spec coverage:** claims #1–#7 → Tasks 1–3, one checkbox each, with the replacement text
  embedded verbatim so the builder pastes rather than re-derives (HR10).
- **Enumeration (HR5):** the stale-claim table is the full set found by auditing all three
  files line-by-line against code; the verified-true list shows what was checked and kept.
- **Figures (HR7):** 7 builders, 6 invoice commands, 286 baseline tests — each with the exact
  command QA re-runs.
- **Gates (HR2):** the four CI-exact commands, stated twice (Global constraints + Task 4).
- **Runbook (HR1):** explicitly not operator-facing; stated in Global constraints and the DONE
  step.
- **Waves (HR11):** no dependency, no wave tag; operator-surfaces adjacency handled in
  Non-goals (different claims, textual rebase only).
- **Deferred:** none.

## HANDOFF NOTES

- Current phase: Task 2 complete and verified (`seven horizon builders` hit at README.md:69; `xsource invoice` count 7; `re-commit` no output; config keys at README.md:142-143).
- Next concrete step: start Task 3 pyproject/docstring updates.
- Decisions taken: used the binding CLAUDE.md replacement verbatim. README wording deviates only to satisfy the plan's verification greps: kept `seven horizon builders` on one line, and used "local commit hooks" instead of "pre-commit hooks" because `grep -n "re-commit" README.md` would otherwise match the pinned wording.
- Known failing tests: none yet.
- Dependencies/operator TODOs: none.
