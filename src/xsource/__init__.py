"""Auto-Procurer (xsource) — a Clonway fleet worker.

Scaffolded from clonway-cockpit's worker template (S8/C6), now running a live
procurement cockpit, composed Signal horizon, telemetry, and the single
write-gate + draft-never-send safety posture.
"""
